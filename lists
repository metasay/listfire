# print(a.append(0)) #append():괄호 안에 값을 리스트의 맨 마지막 에 추가 한다.(추가)
# a.insert(2,b) #자신이 원하는 위치의 자리에 넣는다.(삽입)
# a.extend(b) # 확장한다(확장)
# a.remove(2) # 자신이 정한 숫자를 리스트에서 삭제 한다.
# del a[2] # 그자리의 숫자를 삭제 한다.
# a.pop(1) # 안넣어지면 마지막걸 없애고 인덱스를 넣으면 그자리의 수를 수출 한다.
# print(a.pop())   #(수출한다)
# a.reverse()  #순서 반전
# a.sort()  #숫자를 순서대로 정렬해준다.(오른 차순으로 )
# a.sort(reverse=True)  #숫자를 역순서대로 정렬해준다 (내림 차순으로 )
# print(a)
